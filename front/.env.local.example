# 두 백엔드 오리진 (Next rewrites가 서버 사이드에서 프록시)
MVC_ORIGIN=http://localhost:8000
WEBFLUX_ORIGIN=http://localhost:8001

# 브라우저가 백엔드를 직접 호출해야 할 때만 설정한다. 비어 있으면 동일 오리진 + rewrites 사용.
NEXT_PUBLIC_API_BASE_URL=

# WebSocket 은 rewrites 를 타지 않는다(HTTP 전용) — 브라우저가 WebFlux 오리진에 직접 접속한다.
NEXT_PUBLIC_WS_BASE_URL=ws://localhost:8001

# Google OAuth
NEXT_PUBLIC_GOOGLE_CLIENT_ID=985212099499-uel8drlne8o4et7e3u1gbnjjvdi0k11l.apps.googleusercontent.com
